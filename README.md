# Лендинг для инвестиционного консультанта

Профессиональный одностраничный сайт для привлечения клиентов на бесплатные консультации по инвестициям и личным финансам.

## 🎯 Описание

Лендинг создан для инвестиционных консультантов, которые хотят привлекать клиентов онлайн. Сайт оптимизирован для конверсии и содержит все необходимые элементы для убеждения посетителя записаться на консультацию.

### Основные функции:

- ✅ Современный минималистичный дизайн
- ✅ Полностью адаптивная верстка (мобильные, планшеты, десктоп)
- ✅ Модальная форма записи на консультацию
- ✅ Валидация данных на клиенте и сервере
- ✅ Защита от спама и ограничение частоты запросов
- ✅ Автоматические email-уведомления
- ✅ Админ-панель для управления заявками
- ✅ HTTPS и security headers
- ✅ Анимации при скролле

## 📋 Структура проекта

```
investment-landing/
├── index.html                      # Статическая версия (HTML)
├── css/
│   └── style.css                   # Стили
├── js/
│   └── main.js                     # JavaScript логика
├── wordpress-theme/                # WordPress тема
│   ├── style.css                   # Theme metadata
│   ├── index.php                   # Главный шаблон
│   ├── functions.php               # Функции темы и AJAX handlers
│   └── assets/
│       ├── css/
│       │   └── style.css           # Стили темы
│       └── js/
│           └── main.js             # JavaScript темы
└── README.md                       # Эта инструкция
```

## 🚀 Установка

### Вариант 1: Статический HTML сайт

1. Скопируйте файлы `index.html`, `css/`, `js/` на ваш хостинг
2. Откройте `index.html` в браузере
3. Настройте email в HTML файле (по умолчанию: info@finance-consulting.ru)

**Примечание:** В статической версии форма сохраняет данные в localStorage браузера. Для полноценной работы рекомендуется использовать WordPress версию.

### Вариант 2: WordPress тема (Рекомендуется)

#### Требования:
- WordPress 5.0 или выше
- PHP 7.4 или выше
- MySQL 5.7 или выше

#### Установка:

1. **Загрузите тему:**
   ```bash
   cd wp-content/themes/
   mkdir investment-landing
   # Скопируйте содержимое папки wordpress-theme в investment-landing
   ```

2. **Скопируйте стили:**
   ```bash
   cp css/style.css wordpress-theme/assets/css/style.css
   ```

3. **Активируйте тему:**
   - Зайдите в админ-панель WordPress
   - Перейдите в Внешний вид → Темы
   - Активируйте тему "Investment Consultant Landing"

4. **Проверьте создание таблицы:**
   Тема автоматически создаст таблицу `wp_consultation_requests` при активации

5. **Настройте email:**
   - По умолчанию уведомления приходят на admin_email из настроек WordPress
   - Для изменения адреса отправителя отредактируйте functions.php

## ⚙️ Настройка

### Изменение контента

Все тексты находятся в `index.php` (WordPress) или `index.html` (статическая версия).

#### Основные блоки для редактирования:

1. **Hero секция** - заголовок и подзаголовок
2. **Преимущества** - 4 карточки с преимуществами
3. **Как это работает** - 4 шага процесса
4. **О консультанте** - информация об эксперте
5. **Отзывы** - 3 отзыва клиентов
6. **Футер** - контактный email

### Изменение дизайна

Все стили находятся в `css/style.css` или `wordpress-theme/assets/css/style.css`

#### CSS переменные для быстрой настройки цветов:

```css
:root {
    --primary-color: #2563eb;      /* Основной цвет */
    --primary-dark: #1e40af;       /* Темный оттенок */
    --secondary-color: #10b981;    /* Дополнительный цвет */
    --text-primary: #1f2937;       /* Цвет основного текста */
    --text-secondary: #6b7280;     /* Цвет вторичного текста */
}
```

### Настройка формы

Поля формы можно изменить в модальном окне:

- **Обязательные поля:** Имя, Email, Телефон, Согласие с политикой
- **Необязательное поле:** Комментарий

#### Валидация:

- Имя: минимум 2 символа, только буквы
- Email: стандартная валидация email
- Телефон: минимум 10 цифр, автоформатирование

## 🔒 Безопасность

### Реализованные меры безопасности:

1. **WordPress Nonce** - защита от CSRF атак
2. **Rate Limiting** - ограничение 1 заявка в 5 минут с одного IP
3. **Spam Detection** - проверка на спам-паттерны
4. **Input Sanitization** - очистка всех входных данных
5. **SQL Injection Protection** - использование prepared statements
6. **Security Headers** - X-Frame-Options, X-XSS-Protection и другие
7. **HTTPS Only** - рекомендуется использовать SSL сертификат

### Рекомендации:

- Используйте HTTPS (Let's Encrypt - бесплатно)
- Регулярно обновляйте WordPress и PHP
- Установите плагин для firewall (например, Wordfence)
- Делайте резервные копии базы данных

## 📊 Управление заявками

### Админ-панель WordPress

После установки в меню WordPress появится раздел "Заявки":

1. **Просмотр всех заявок** - таблица со всеми запросами
2. **Информация о клиенте** - имя, email, телефон, комментарий
3. **Статусы заявок:**
   - 🔵 Новая
   - 🟡 В работе
   - 🟢 Завершена
   - 🔴 Отменена

4. **Действия:**
   - Изменение статуса заявки
   - Клик на email для быстрой отправки письма
   - Фильтрация и поиск (можно добавить при необходимости)

### Email уведомления

#### Уведомление администратору:
- Отправляется сразу после заполнения формы
- Содержит все данные клиента
- Включает IP адрес и время отправки

#### Подтверждение клиенту:
- Автоматическое письмо с благодарностью
- Информация о дальнейших действиях
- Контактные данные для связи

### Экспорт данных

Для экспорта заявок из базы данных:

```sql
SELECT * FROM wp_consultation_requests 
ORDER BY created_at DESC;
```

Можно использовать phpMyAdmin или плагины для экспорта в CSV/Excel.

## 🎨 Кастомизация

### Добавление изображений

Замените placeholder'ы на реальные изображения:

1. **Hero изображение** - добавьте `<img>` вместо `.hero-placeholder`
2. **Фото консультанта** - замените `.expertise-placeholder`
3. **Favicon** - добавьте в `<head>`:
   ```html
   <link rel="icon" type="image/png" href="favicon.png">
   ```

### Интеграция с CRM

Для интеграции с внешней CRM (например, amoCRM, Битрикс24):

Отредактируйте функцию `investment_landing_save_consultation()` в `functions.php`:

```php
// После успешного сохранения в БД
if ($result !== false) {
    // Отправка в CRM
    send_to_crm($name, $email, $phone, $comment);
}
```

### Добавление Google Analytics

Добавьте код отслеживания в `<head>`:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

### Отслеживание конверсий

Добавьте событие при успешной отправке формы в `main.js`:

```javascript
if (result.success) {
    // Google Analytics Event
    gtag('event', 'consultation_request', {
        'event_category': 'Form',
        'event_label': 'Consultation Form'
    });
    
    // Facebook Pixel
    fbq('track', 'Lead');
}
```

## 🧪 Тестирование

### Проверка работы формы:

1. Откройте сайт
2. Нажмите "Записаться на встречу"
3. Заполните форму тестовыми данными
4. Проверьте валидацию (попробуйте некорректные данные)
5. Отправьте форму
6. Проверьте:
   - Сообщение об успехе
   - Email уведомления
   - Запись в админ-панели WordPress

### Тестирование безопасности:

```bash
# Проверка rate limiting
curl -X POST http://your-site.com/wp-admin/admin-ajax.php \
  -d "action=save_consultation_request&name=Test&email=test@test.com&phone=1234567890"

# Повторите запрос - должен быть заблокирован
```

## 📱 Адаптивность

Лендинг полностью адаптивен для всех устройств:

- **Desktop** (1200px+) - полная версия с grid layouts
- **Tablet** (768px - 1199px) - адаптированная версия
- **Mobile** (до 767px) - мобильная версия с вертикальным расположением блоков

## 🐛 Решение проблем

### Форма не отправляется

1. Проверьте консоль браузера (F12) на наличие ошибок JavaScript
2. Убедитесь, что WordPress активирован и работает
3. Проверьте, что таблица `wp_consultation_requests` создана в БД
4. Проверьте права доступа к файлам (должны быть 644 для файлов, 755 для папок)

### Email не приходят

1. Проверьте настройки SMTP в WordPress
2. Установите плагин для email (WP Mail SMTP)
3. Проверьте спам-папку
4. Убедитесь, что хостинг поддерживает отправку email

### Стили не применяются

1. Очистите кэш браузера (Ctrl+F5)
2. Проверьте путь к CSS файлу
3. Убедитесь, что файл `style.css` скопирован в `assets/css/`
4. Проверьте консоль на ошибки загрузки ресурсов

### База данных

Если таблица не создалась автоматически, выполните SQL вручную:

```sql
CREATE TABLE IF NOT EXISTS wp_consultation_requests (
    id bigint(20) UNSIGNED NOT NULL AUTO_INCREMENT,
    name varchar(200) NOT NULL,
    email varchar(200) NOT NULL,
    phone varchar(50) NOT NULL,
    comment text,
    ip_address varchar(100),
    user_agent text,
    created_at datetime DEFAULT CURRENT_TIMESTAMP,
    status varchar(50) DEFAULT 'new',
    PRIMARY KEY (id),
    KEY email (email),
    KEY created_at (created_at),
    KEY status (status)
);
```

## 📈 Оптимизация конверсии

### Рекомендации для улучшения конверсии:

1. **A/B тестирование** - тестируйте разные заголовки и CTA кнопки
2. **Социальные доказательства** - добавьте реальные отзывы с фото
3. **Urgency** - добавьте ограниченное количество бесплатных консультаций
4. **Trust badges** - добавьте сертификаты и лицензии
5. **Video** - добавьте короткое видео-приветствие
6. **Live chat** - интегрируйте чат для мгновенной связи

## 🔄 Обновления

Для обновления темы:

1. Сделайте резервную копию текущей версии
2. Скопируйте новые файлы
3. Проверьте, что все кастомизации сохранены
4. Очистите кэш

## 📞 Поддержка

При возникновении проблем:

1. Проверьте раздел "Решение проблем"
2. Посмотрите логи ошибок WordPress (wp-content/debug.log)
3. Проверьте консоль браузера на JavaScript ошибки

## 📄 Лицензия

Этот проект создан для использования в коммерческих целях. Вы можете свободно модифицировать и использовать код для своих проектов.

## ✅ Чеклист запуска

Перед запуском убедитесь:

- [ ] WordPress установлен и работает
- [ ] Тема активирована
- [ ] CSS файл скопирован в assets/css/
- [ ] Таблица в БД создана
- [ ] Email уведомления настроены
- [ ] SSL сертификат установлен (HTTPS)
- [ ] Тестовая заявка отправлена успешно
- [ ] Email приходят на admin_email
- [ ] Админ-панель "Заявки" отображается
- [ ] Мобильная версия протестирована
- [ ] Google Analytics подключен (опционально)
- [ ] Политика конфиденциальности создана

---

**Готово! Ваш лендинг готов привлекать клиентов. Успехов! 🚀**
